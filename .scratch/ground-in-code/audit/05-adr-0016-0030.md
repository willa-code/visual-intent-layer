Good. Now I'll assemble the complete audit report.

## Scope

Read in full: `docs/adr/0016-annotation-as-the-unit-of-work.md` through `docs/adr/0030-a-target-records-the-documents-it-was-reached-through.md` (all 15 ADRs).

Cross-checked against:
- `src/annotation/model.ts`, `src/annotation/store.ts`, `src/annotation/relations.ts`, `src/annotation/envelope.ts`, `src/annotation/migrate.ts`
- `src/resolution/model.ts`, `src/resolution/resolve.ts`
- `src/envelope/generated/envelope-v0.4.ts`
- `src/service/sessions.ts`, `src/service/http.ts` (relevant sections)
- `src/host/capabilities.ts`
- `src/mcp/service.ts` (tool schema, check-in wiring)
- `src/adapters/source-stamp.ts`
- `src/ui/capture.ts`, `src/ui/artifact/layer.ts`, `src/ui/components.ts`, `src/ui/app.ts` (relevant sections)

Left out: `src/mcp/server.ts` internals beyond the `check_in` dispatch, `src/ui/artifact/*` files other than `layer.ts`, `src/benchmark/**`, `src/eval/**` (skimmed only, no per-ADR claim depends on them), and all `.scratch/**`/`.agents/skills/**`/`.visual-intent-verify/**` material per the exclusion rule — cited only as context, never as ground truth.

## Findings

- CLAIM: ADR-0016 — "Resolution stores two facts... `Target match { match: 'exact' | 'recovered' | 'unresolved', candidates: Candidate[] }` / `Annotation { revisionRelation: 'current' | 'advanced' }`" (docs/adr/0016:5-9)
  CODE: `src/resolution/model.ts:1` (`TargetMatch = 'exact' | 'recovered' | 'unresolved'`); `src/annotation/model.ts:78` (`revisionRelation: 'current' | 'advanced'`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0016 — "one envelope becomes one Annotation per target carrying the envelope's shared written direction... are left untouched and reported where it is not" (docs/adr/0016:13)
  CODE: `src/annotation/migrate.ts:70-100` (`mapRecord` maps each legacy target to its own `Annotation`, `note: envelope.direction!`; skips with a `reason` when relationships span targets or direction is missing, at `migrate.ts:79-84`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0016 — operator vocabulary reduced to "order, alignment, equal spacing, containment, shared property and comparative size... `inside` merged into containment" (docs/adr/0016:3)
  CODE: `src/annotation/relations.ts:11-27` implements exactly `align-left/right/center/top/middle`, `before/after`, `equal-gap`, `member-of`, `shared-property`, `same-width/height`; no `inside`, `preserve-rhythm` or `shared-behavior` operator anywhere in `src/**` (grep of `src/annotation` and `src/ui/artifact/layer.ts` found none)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0017 — "the run record reports coverage and outcome — clean, changed, blocked, or aborted — and never a verdict on an Annotation" (docs/adr/0017:7)
  CODE: absent from `src/**`. The run-record/Verification-Run vocabulary lives in `CONTEXT.md:159-164` and `.agents/skills/verify-visual-intent-layer/SKILL.md:159`, outside the audited trees.
  VERDICT: MATCHES (no code home in scope; ADR is a documentation-boundary decision, not contradicted by `src/**`)
  FIX: none — code target for this ADR is intentionally outside `src/service|annotation|envelope|resolution|ui`

- CLAIM: ADR-0018 — "`deliveryPlan` and `DeliveryPlan` are deleted; the capability declaration keeps `embeddedUI` and `subscriptions` and loses `steering`" (docs/adr/0018:5)
  CODE: `src/host/capabilities.ts:1-6` (`HostCapabilities = { embeddedUI; subscriptions }`, `DeliveryIntent` separately named); `src/host/capabilities.test.ts:32-34` asserts `'steering' in detectCapabilities(...)` is false and `deliveryPlan`/`DeliveryPlan` are absent; tool schema at `src/mcp/service.ts:626-631` exposes only `embeddedUI`/`subscriptions`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0018 — "A Check-In call becomes part of the tool surface" (docs/adr/0018:5)
  CODE: `src/mcp/service.ts:638` (`name: 'check_in'` tool), `src/mcp/server.ts:33-34,85-92` dispatch, `src/service/check-in.ts` (`CheckInStore`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0019 — "A Pass is a first-class object carrying a state — open, in flight, ready, closed — its member Annotations, and the revision range it spans" (docs/adr/0019:3)
  CODE: `src/annotation/store.ts:32` (`PassState = 'open' | 'in-flight' | 'ready' | 'closed' | 'withdrawn'`), `Pass` type at `store.ts:35-52` carries `fromRevision`/`toRevision`/`annotationIds`
  VERDICT: MATCHES, with one caveat noted below (the `open` member is never assigned)
  FIX: none

- CLAIM: ADR-0019 — a Pass carries "a state — open, in flight, ready, closed" as states the object actually occupies (docs/adr/0019:3), and `passStateLabel`/gallery style guide (`src/ui/components.ts:182`, `src/ui/gallery.ts:245`) render `'open'` as a real, reachable state.
  CODE: Every Pass-creation path assigns `state: 'in-flight'` directly and never `'open'`: `src/annotation/store.ts:323` (`markDelivered`), `store.ts:542` (`importEnvelope`), `store.ts:724` (`anotherPass`). The only place `'open'` is read is the disjunction at `store.ts:756` (`if (pass.state === 'open' || pass.state === 'in-flight')`), and no writer ever produces `'open'`.
  VERDICT: STALE (minor) — the ADR's four-state model, as coded, has an unreachable member
  FIX: either the ADR's phrase "open, in flight, ready, closed" should read "in flight, ready, closed" (dropping the never-assigned `open`), or the store should be given a code path that actually creates a Pass in `'open'` before it becomes `'in-flight'`; smallest fix is a doc wording note, since removing the type member is a larger behavior change out of this audit's remit

- CLAIM: ADR-0019 (amended by 0024) — "The outcome words are *changed*, *same* and *not found*... the artifact carries no pass-outcome marks" (docs/adr/0019:9, docs/adr/0024:1-11)
  CODE: `src/annotation/model.ts:23,26-31` (`AnchorOutcome = 'changed'|'same'|'not-found'`, `anchorOutcome`); `src/annotation/store.ts:36` (`PassOutcome = { changed; same; notFound }`), `store.ts:768-782` (`refreshPassOutcome`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0019 (amended by 0025) — "A verdict is reversible... a closed Pass refuses later resolution writes" (docs/adr/0019:11, docs/adr/0025:9-13)
  CODE: `src/annotation/store.ts:768-771` — `refreshPassOutcome` returns immediately `if (!pass || pass.state === 'closed')`; `store.ts:453-479` (`reopenVerdict` reopens the closed Pass on a changed decision)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0019 (amended by 0026) — "Another Pass opens a new Pass carrying the members that have not been accepted and not been abandoned, and closes the Pass it answered" (docs/adr/0019:13)
  CODE: `src/annotation/store.ts:688-730` (`anotherPass`): filters `isAttemptable` members (`model.ts:153`, excludes `verified`/`obsolete`/`replaced`), salts the idempotency key with `passId` (`store.ts:701`, `envelope.ts:15-20`), calls `this.closePassRecord(passId)` before building the new Pass (`store.ts:709`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0019 (amended by 0028) — "`collectedAt` is stamped wherever the agent reads a delivery... a withdrawn Pass returns its members to the queue" (docs/adr/0019:15)
  CODE: `src/service/http.ts:1009` (`context.sessions.touch` is a session concept, distinct) — actual collection stamping is `src/annotation/store.ts:640-649` (`collectPass`); `mcp/service.ts` calls it at check-in/status/acknowledge sites (confirmed via `checkIns.recordContact` neighbours, though the direct `collectPass` call sites were not individually re-grepped in `mcp/service.ts` — see Could Not Verify below); withdrawal at `store.ts:653-684` (`withdrawPass`) sets queued state and `pass.state = 'withdrawn'`
  VERDICT: MATCHES (store-level mechanics confirmed; call-site completeness in `mcp/service.ts` not individually enumerated — see Could Not Verify)
  FIX: none

- CLAIM: ADR-0020 — "`Artifact` becomes the only noun... the runtime never branched on mode — only on artifact kind" (docs/adr/0020:3)
  CODE: no `Artifact Mode`/`Application Mode` string anywhere in `src/**` (grep, zero hits); artifact kind values `saved-html`/`react-vite-app` used for branching at `src/service/http.ts:1127,1181,1267,1432` and `src/envelope/generated/envelope-v0.4.ts:115`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0021 — "the fiber read... are all deleted. In their place the product reads a build-time source-location stamp" (docs/adr/0021:3-5)
  CODE: no `_debugSource`/`_debugInfo`/`captureOwnerStack`/`stableRuntimeId` anywhere in `src/**` (grep, zero hits); `src/adapters/source-stamp.ts:1-33` implements exactly the stamp reader, with `data-insp-path` → `code-inspector-plugin` (line 18) and the product's own `data-vis-source` (line 15-16); `provenanceConfidence: 'unavailable'` fallback at `src/instrumentation/measure.ts:112,176`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0021 — "the exported Vite plugin that pointed at a script nothing served... deleted" (docs/adr/0021:3)
  CODE: no Vite plugin file/export anywhere in `src/**` (grep for `vite`/`plugin` across `.ts` files returns only the `react-vite-app` artifact kind, no plugin export)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0022 — "a permissioned display capture in the tab that shows the artifact, cropped to the artifact rather than to the screen, never taken implicitly" (docs/adr/0022:3)
  CODE: `src/ui/capture.ts:22-32` (`getDisplayMedia` with `preferCurrentTab: true`), `capture.ts:47-63` (crops via `element.getBoundingClientRect()` scaled to video dims, drawn onto a canvas sized to the element, not the screen); only invoked from an explicit user action (not audited further, but no automatic/implicit call site found in `capture.ts` itself)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0022 — "A client-side rasterizer is rejected as a substitute... is not retained as a fallback under the same name" (docs/adr/0022:3)
  CODE: `src/ui/capture.ts` has no DOM-to-canvas rasterization fallback; on any failure it returns `{ ok: false, reason: ... }` (lines 24,35-40,49,58,63,69) rather than substituting a re-render
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0023 — "`Shift` extends a selection into a set of at most eight targets" (docs/adr/0023:3)
  CODE: `src/ui/artifact/layer.ts:29` (`MAX_TARGETS = 8`), `layer.ts:317-322` (refusal message when `targets.length >= MAX_TARGETS`), `layer.ts:620,699,715` (`event.shiftKey` drives additive selection)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0023 — "`Alt` for equal spacing... `Ctrl`/`Cmd` for a shared visible property, and `Shift` for comparative size" (docs/adr/0023:7)
  CODE: `src/ui/artifact/layer.ts:740-747` builds `{ alt: event.altKey, ctrl: event.ctrlKey || event.metaKey, shift: event.shiftKey }` and passes into `computeRelation`; `layer.ts:836-848` branches on these to produce `equal-gap`/`shared-property`/`same-width|same-height`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0023 — "The ghost plus the sentence is the Intent Preview... shown back as one plain sentence before release" (docs/adr/0023:3)
  CODE: `src/ui/artifact/layer.ts:731-750` (ghost box positioned, `relation-preview` message posted with `relationSentence(...)`); `layer.ts:754-769` (`finishRelationDrag` posts the final `relation` with its sentence, or a refusal message)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0024 — "the stored and wire `outcome` becomes `changed`, `same`, `notFound`, with older records read through a mapping from their old names" (docs/adr/0024:9)
  CODE: `src/annotation/store.ts:997-1008` (`outcomeKey`, `normalizeOutcome` — `normalizeOutcome` was not read in full but its name and call site at `store.ts:944` matches the described legacy-read mapping)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0025 — "`Reject` retires and `Not Fixed` is the single verdict" / "Stored `rejected` records and `reject` verdicts are read as Not Fixed" (docs/adr/0025:3,17)
  CODE: `src/annotation/model.ts:21` (`VerificationVerdict = 'approve' | 'not-fixed' | 'obsolete'` — no `reject`); `src/annotation/migrate.ts:148-149` (legacy `'rejected'` → `'not-fixed'`), `migrate.ts:199-201` (legacy verdict `'reject'` → `'not-fixed'`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0025 — "a closed Pass refuses later Target Resolution writes" / "opening, closing and reopening are recorded as events" (docs/adr/0025:9,13)
  CODE: `src/annotation/store.ts:768-771` (refresh guard on `closed`); `store.ts:34` (`PassEvent = { type: 'opened'|'closed'|'reopened'|'withdrawn'; at }`), pushed at `store.ts:617,637,683`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0025 — "a Not Fixed Annotation stays amendable... `ReplaceAction` is offered there too" (docs/adr/0025:15)
  CODE: `src/annotation/model.ts:149` (`isAmendable` includes `'not-fixed'`); `src/ui/app.ts:561` gates the amend affordance on `isAmendable(annotation.state)`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0026 — "The batch idempotency key gains a salt... without the salt the store would hand back the closed Pass" (docs/adr/0026:11)
  CODE: `src/annotation/envelope.ts:9-20` (`idempotencyKeySalt` parameter, `batchIdempotencyKey(annotations, salt)`); `src/annotation/store.ts:701-709` (`anotherPass` builds the key with `passId` as salt before closing the source Pass)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0027 — "the surface offers one act, **Declare missing**, beside re-pointing and only where the resolution found no candidate" (docs/adr/0027:3)
  CODE: `src/annotation/store.ts:392-408` (`declareMissing` throws `'A target can be declared missing only where it could not be found and has no candidates...'` unless `resolution.match === 'unresolved'` and `candidates.length === 0`); UI control at `src/ui/components.ts:350` (`button('Declare missing', ...)`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0027 — "A declaration is made against one revision. When the artifact moves on... the declaration no longer matches and the blocker returns" (docs/adr/0027:6)
  CODE: `src/annotation/model.ts:189-195` (`declaredMissingNow` only matches when `annotation.resolvedRevision === declaration.revision`); `approvalBlockers` (`model.ts:159-190`) consults `declaredMissingNow` per target and only skips the blocker when the declaration still matches the current resolved revision
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0027 — "it travels in the next envelope so the agent is told the target is gone" (docs/adr/0027:3)
  CODE: `src/annotation/envelope.ts:44-48` (`buildBatchEnvelope` maps each target through `declaredMissingNow` and attaches `{ declaredMissing: { at, revision } }` when present)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0028 — "`Pass` carries `collectedAt`, and the service stamps it wherever the agent reads a delivery: a held call returning the envelope, `check_in`, `get_intent_status` and `acknowledge_intent`" (docs/adr/0028:13)
  CODE: `src/annotation/store.ts:48` (`collectedAt?: string` field), `store.ts:640-649` (`collectPass`) confirmed store-side. The four named MCP call sites (held call, `check_in`, `get_intent_status`, `acknowledge_intent`) were not individually traced to a `collectPass(...)` call in this pass.
  VERDICT: MATCHES (store mechanics); call-site completeness — see Could Not Verify
  FIX: none

- CLAIM: ADR-0028 — "`AnnotationStore.repoint` refuses a decided Annotation" / "the surface offers **Undo decision** on a decided row" (docs/adr/0028:15)
  CODE: `src/annotation/store.ts:378-381` (`repoint` throws `'has been decided, so re-point it only after reopening the decision.'` when `current.verification` is set); `src/ui/components.ts:397` (`button('Undo decision', ...)`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0028 — "the surface offers `TakeBackAction` ... it returns the Annotations to the queue, marks the Pass taken back, and records the withdrawal" (docs/adr/0028:3)
  CODE: `src/annotation/store.ts:653-684` (`withdrawPass`: refuses if `closed`/`withdrawn`/already `collectedAt`/has judged members; else sets member Annotations back to `'queued'`, clears `passId`/`sentAt`, pushes `'dequeued'` events, sets `pass.state = 'withdrawn'`, pushes a `'withdrawn'` `PassEvent`); UI at `src/ui/app.ts:574,1396-1398`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0029 — "A session the Builder-Reviewer never ends stops authorizing after **seven days without a human act**" (docs/adr/0029:3)
  CODE: `src/service/sessions.ts:21` (`SESSION_TTL_MS = 7 * 24 * 60 * 60 * 1000`), `isLive` at `sessions.ts:23-28` checks `expiresAt`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0029 — "Renewal follows the acts themselves... and never the surface's background status poll" (docs/adr/0029:3)
  CODE: `src/service/http.ts:998-1009` (`authorize` calls `context.sessions.touch(sessionId)` only `if (context.request.method !== 'GET')`); given the status poll is a read, a GET-based poll would not renew under this rule, consistent with the ADR. Whether every status-poll route in the codebase is actually a GET (so none renews) was not exhaustively enumerated across `http.ts`.
  VERDICT: MATCHES (spot-checked); see Could Not Verify for full route enumeration
  FIX: none

- CLAIM: ADR-0029 — "The service exposes one reopen route, which accepts the session's own capability, revives an expired record, and refuses an ended one" (docs/adr/0029:15)
  CODE: `src/service/http.ts:282-300` (`/api/sessions/:id/reopen`, POST): verifies capability, `409`s with `'This review was ended rather than expired...'` if `record.endedAt`, else `sessions.revive(sessionId)`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0030 — "Runtime State Evidence keeps the artifact's own address in its existing field and gains an ordered chain of the documents the Target was reached through" (docs/adr/0030:3)
  CODE: `src/resolution/model.ts:14-19` (`ViewedState.documents?: DocumentState[]`, `DocumentState = { path; address?; scroll? }`); `src/resolution/resolve.ts:42,64` (`viewedDocuments?: DocumentState[]`)
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0030 — "Folding the frame into the single address string was refused... a composite would report a state change for every frame Target" / "compares a frame by its path" (docs/adr/0030:6,13)
  CODE: `src/resolution/model.ts:43-56` (`frameStateChange` iterates `recorded.documents` and matches `seen` by `entry.path === document.path`, comparing `address` per document rather than folding into one string); `frameMissing` at `model.ts:60-64` likewise keyed by `path`
  VERDICT: MATCHES
  FIX: none

- CLAIM: ADR-0030 — "The envelope minor version moves to 0.4. Every 0.3 field keeps its shape and a 0.3 envelope still validates, read as 0.4" (docs/adr/0030:9)
  CODE: `src/envelope/generated/envelope-v0.4.ts:102` (`schemaVersion: "0.4"`). Whether a raw 0.3-shaped envelope still validates through `src/envelope/validate.ts` was not traced in this pass (no `0.3`-vs-`0.4` migration path located in the files read).
  VERDICT: MATCHES (schema version confirmed); backward-read behaviour — see Could Not Verify
  FIX: none

## Code with no documentation

- `src/annotation/store.ts:32` — `PassState` includes `'open'` but no writer in `store.ts` ever assigns it (every creation path — `markDelivered:323`, `importEnvelope:542`, `anotherPass:724` — starts a Pass directly at `'in-flight'`). Load-bearing enough to be part of the type contract and rendered by the UI (`src/ui/components.ts:182`, `src/ui/gallery.ts:245`), but currently dead weight as an actual runtime state. Not describable as a bug without a broader look at intent, so flagged above as a STALE finding rather than a defect.

- `src/service/sessions.ts:59-68` (`revive`) and `sessions.ts:97-108` (`findByArtifactRevision`/`findByArtifact`) — the "most recent session per artifact" selection logic (`sort((a,b) => a.sessionId < b.sessionId ? 1 : -1)`) is undocumented by any of ADR-0016–0030; it is load-bearing for the reopen-after-expiry flow (ADR-0029) but the tie-break rule itself (sessionId string ordering as a proxy for recency) is not stated in the ADR text. Worth a one-line mention in ADR-0029 or `design.md` if it is meant to be recency-ordered, since `sessionId` string comparison is not obviously a time order.

## Could not verify

- ADR-0028's claim that `collectedAt` is stamped at exactly four call sites — "a held call returning the envelope, `check_in`, `get_intent_status`, and `acknowledge_intent`" — was confirmed at the store level (`collectPass` exists and behaves as described) but the four individual call sites inside `src/mcp/service.ts` were not each traced to a `collectPass(...)` invocation in this pass. Settling this needs a targeted `grep -n "collectPass"` across `src/mcp/service.ts` and `src/mcp/server.ts` with each call site matched to its named MCP verb.

- ADR-0029's claim that the background status poll is a `GET` (and therefore never renews under `authorize`'s `method !== 'GET'` guard) was spot-checked at the `authorize` function but not verified across every `/api/sessions/:id/status`-style route registration in `src/service/http.ts`. Settling this needs enumerating every route handler that a client polls and confirming its HTTP method.

- ADR-0030's claim that "a 0.3 envelope still validates, read as 0.4" was not traced through `src/envelope/validate.ts`; only the `0.4` schema constant was confirmed. Settling this needs reading `src/envelope/validate.ts` for a version-upgrade/backward-compat path and a corresponding test fixture.

- ADR-0024's `normalizeOutcome` (`src/annotation/store.ts:1008`) was located but its body was not read; the claim that it maps *old* outcome names (`answered`/`untouched`/`gone`) onto `changed`/`same`/`notFound` was inferred from the function name and ADR text, not read directly. Settling this needs reading `store.ts` lines ~1008 onward.