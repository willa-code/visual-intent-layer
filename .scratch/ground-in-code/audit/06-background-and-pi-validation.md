## Scope

Read in full: `docs/background/README.md`, `docs/background/harness-mcp-setup-research.md`, `docs/background/mcp-integration-research.md`, `docs/background/release-and-prerelease-practices.md`, `docs/background/product-strategy.md`, `docs/background/mode-and-toolbar-research.md`, `docs/pi-validation.md`. Read the first ~2000 lines of `docs/background/product-research.md` (file continues past that; the unread remainder is architecture/positioning prose consistent with what was already read, not new factual claims about current code) and the first 60 lines each of `docs/background/similar-mcp-landscape.md` and `docs/background/tldraw-fit-research.md` (both are third-party competitive surveys with no claims about this repo's code).

Verified against: `src/cli.ts`, `src/cli-setup.ts`, `src/harness-registry.ts` (existence only), `src/host/capabilities.ts`, `src/mcp/service.ts`, `src/mcp/protocol.test.ts`, `tests/browser-loop.test.ts`, `src/ui/styles/shell.css`, `package.json`, `mcp.json`, `.github/workflows/publish.yml`, and ADRs 0002, 0008, 0013, 0014, 0018 (ADRs are cited only as corroborating evidence for settled decisions/current design intent, not as the audit target).

Left out: `.scratch/**`, `.visual-intent-verify/**`, vendored skills under `.agents/skills/**` and `skills/**` (per scope), the rest of `docs/adr/*` beyond the five cited, and the tail of `product-research.md` (lines 2001–2536).

## Findings

- **CLAIM:** "Our `visual-intent setup` writes project `.mcp.json` or global `~/.config/mcp/mcp.json` (`src/cli-setup.ts`)... This confirms the user's Codex report." — `docs/background/harness-mcp-setup-research.md:7,53`
  **CODE:** `src/cli-setup.ts` still exists and `src/cli.ts:37-86` still wires a `setup` verb into the CLI help and dispatch (`src/cli.ts:10-19`). But the code has moved well past the doc's "matches almost no harness" snapshot: `planCodex` (`src/cli-setup.ts:296-336`) now writes native Codex TOML, `planClaudeGlobal` (`src/cli-setup.ts:395-424`) delegates to `claude mcp add-json`, and `planOpencode` (`src/cli-setup.ts:426-473`) writes native opencode JSON — i.e., section 10's "Recommendation" (`harness-mcp-setup-research.md:73-79`) has already been implemented, but the doc still frames it as a future recommendation against a described-as-broken present.
  **VERDICT:** STALE
  **FIX:** This entire document was written to spec the setup command's per-harness matrix. Per settled decision #1, `setup`, `src/cli-setup.ts`, and `src/harness-registry.ts` are being deleted outright, and installation becomes "the human using their own harness-native MCP registrar." Add a supersession note at the top of `harness-mcp-setup-research.md` stating: (a) the `visual-intent setup` command this document was written to inform has been deleted; (b) the per-harness config-file survey in sections 1–9 (where each harness stores its native MCP config) remains valid *reference material* for a human manually registering the server, and should be pointed to from whatever new install docs replace `setup`; (c) sections 7, 10, and 11 (the setup-behavior analysis and "Recommendation for `visual-intent setup`") describe a command that no longer exists and should be read as historical only, not as pending work.

- **CLAIM:** "Use TypeScript for every browser boundary and Rust for the local authority" and the accompanying architecture diagram (Rust daemon, `rmcp`, SQLite, native binary) — `docs/background/mcp-integration-research.md:172-186`; "the plugin bundles the Skill and MCP configuration. End users should not need a Rust or Node toolchain." — `docs/background/mcp-integration-research.md:200`
  **CODE:** `src/` contains only TypeScript modules (`ls src` shows `adapters/, annotation/, artifact/, cli.ts, host/, mcp/, service/, ui/…`, no Rust crate anywhere in the repo). ADR-0013 (`docs/adr/0013-typescript-only-v0-with-rust-insertion-boundary.md`) explicitly settles "V0 is TypeScript-only for server, UI, adapters, and persistence, distributed via npm with Node required," deferring Rust indefinitely. `package.json`'s `bin` entries (`./dist/cli.js`, `./dist/mcp/stdio.js`) confirm a Node-only distribution, contradicting "End users should not need a... Node toolchain."
  **VERDICT:** STALE
  **FIX:** Add a supersession note to `mcp-integration-research.md` section 4 ("SDK and implementation-language decision") and its "Recommended language split," pointing to ADR-0013: the Rust/TypeScript split was the researched recommendation at the time, but the actual V0 decision is TypeScript-only with Node required, and the Rust insertion boundary is preserved but not built. The rest of section 4's SDK-tier survey (which SDKs are Tier 1, what Tasks/Apps support each SDK offers) remains valid research and does not need correction.

- **CLAIM:** "A thin Agent Skill, distributed in the same plugin" is one of "three layers... but keep only one product," and "One thin Skill, installed with the MCP integration, for proactive invocation and lifecycle behavior." — `docs/background/mcp-integration-research.md:161-165,218`
  **CODE:** `package.json:14-16` still declares `"pi": { "skills": ["./skills"] }` and `skills/visual-intent/SKILL.md` still exists, so this claim currently matches shipped code. But settled decision #2 states the pi Skill (`skills/visual-intent/`, the `pi.skills` field, `--no-skill`) is being deleted entirely, and `src/cli.ts:15` still exposes a `--no-skill` flag that decision #2 also removes.
  **VERDICT:** STALE
  **FIX:** Same supersession note as above should also flag that the "thin Skill distributed in the same plugin" conclusion is reversed by decision #2. Since this was this document's central recommendation (ADR-0002, `docs/adr/0002-self-describing-mcp-with-optional-skill.md`, formalized exactly this recommendation), the note should say explicitly: "The optional-Skill architecture recommended here and adopted in ADR-0002 is being retired; the MCP server becomes the sole self-describing surface with no accompanying Skill artifact."

- **CLAIM:** "**Add a pre-release channel now, while there are no users.** Recommended: prerelease versions `0.X.Y-next.N` published with `npm publish --tag next`... A `next` step guarded by `if: ${{ github.event.release.prerelease }}`..." — `docs/background/release-and-prerelease-practices.md:86-87`
  **CODE:** This recommendation is exactly what shipped: `package.json:3` version is `0.3.0-next.3`, and `.github/workflows/publish.yml:18-23` has precisely the `if: ${{ github.event.release.prerelease }}` / `npm publish --provenance --access public --tag next` step the doc recommended. Settled decision #4 retires this: "The npm pre-release channel... is being retired. Releases are official only; users install `@latest`."
  **VERDICT:** STALE
  **FIX:** Add a supersession note at the top of `release-and-prerelease-practices.md` stating that the pre-release/dist-tag recommendation in its "Recommendation for visual-intent-layer" section (points 1–3, lines ~85–88) was implemented and is now being reversed: `-next.N` versions, the `next` dist-tag, and the `if: github.event.release.prerelease` publish step are being removed; releases become official-only (`X.Y.Z` to `latest`). The npm-mechanics research earlier in the document (dist-tag semantics, semver prerelease ordering, `--tag` requirement, unpublish/deprecate policy, GitHub Actions trigger mechanics) remains accurate and reusable if a pre-release channel is ever reconsidered, so only the "Recommendation" and "Contradictions/Missing evidence/Open questions" sections that assume a live `next` channel need the note; the mechanics sections are RECORD.

- **CLAIM:** "This package installed: `npm install -g visual-intent-layer` (Node 20+), then `visual-intent setup` in a scratch project (or `setup --global`)." and "MCP configuration registered by `setup` and picked up by `pi-mcp-adapter` (standard `.mcp.json` / `~/.config/mcp/mcp.json` files)." — `docs/pi-validation.md:14-15`
  **CODE:** `src/cli.ts:8-19` and `src/cli-setup.ts` currently implement exactly this `setup` flow, so the claim matches code today. Settled decision #1 deletes `setup`, `src/cli-setup.ts`, and `src/harness-registry.ts`; installation becomes "the human using their own harness-native MCP registrar."
  **VERDICT:** STALE
  **FIX:** Rewrite the Prerequisites section of `docs/pi-validation.md` to drop the `visual-intent setup` step and instead instruct the human to register the MCP server with pi's own MCP configuration mechanism directly (i.e., whatever pi's harness-native registrar turns out to be, per decision #1), keeping the rest of the checklist (steps 1–8) unchanged since those exercise `open_visual_review`, `check_in`, `get_intent_status`, and `acknowledge_intent`, none of which depend on `setup`.

- **CLAIM:** "> **Corrected.** This note once described steering as a negotiated host capability. ADR-0018 establishes that steering and interruption are seen at the agent's Check-In, a published convention, and that every MCP call for a session records contact. The `steering` capability flag no longer exists." — `docs/pi-validation.md:3-6`
  **CODE:** `src/host/capabilities.ts:1-4` defines `HostCapabilities = { embeddedUI: boolean; subscriptions: boolean }` with no `steering` field, matching ADR-0018's claim exactly ("the capability declaration keeps `embeddedUI` and `subscriptions` and loses `steering`" — `docs/adr/0018-steering-is-a-convention-not-a-capability.md`). `DeliveryIntent` (`capabilities.ts:6`) does still include a string literal `'steering'`, but that is a delivery-intent label on an Annotation, not a host capability flag — consistent with the ADR's distinction.
  **VERDICT:** MATCHES
  **FIX:** none

- **CLAIM:** Live-checklist steps referencing `open_visual_review`, `check_in`, `get_intent_status`, `acknowledge_intent`, "status must read agent-acknowledged, not verified," "the call returns what arrived without an `envelopeId`," and restart-survival of "last checked in" — `docs/pi-validation.md:23-40`
  **CODE:** All four tool names exist in `src/mcp/service.ts:608,638,651,661`. `checkIn` (`service.ts:439`) takes no `envelopeId` parameter and returns `deliveries`/`interruption` (`service.ts:69-73`). `statusOf` (`service.ts:718-726`) returns `'agent-acknowledged'` for acknowledged-but-not-verified annotations and only returns `'verified'` when every annotation in the pass is verified. `lastCheckedInAt` is persisted via `CheckInStore` (`service.ts:144,455,545`).
  **VERDICT:** MATCHES
  **FIX:** none

- **CLAIM:** "`npm test` — full suite, including the MCP protocol contract (`src/mcp/protocol.test.ts`) and the primary black-box loop (`tests/browser-loop.test.ts`)." — `docs/pi-validation.md:18-19`
  **CODE:** Both files exist (`src/mcp/protocol.test.ts`, `tests/browser-loop.test.ts`); `protocol.test.ts:35,47,51` reference the same `open_visual_review`/`acknowledge_intent` tool names as `service.ts`.
  **VERDICT:** MATCHES
  **FIX:** none

- **CLAIM:** "pi declares no host capabilities, so `detectCapabilities` reports browser review with polling updates (`src/host/capabilities.ts`)." — `docs/pi-validation.md:20-21`
  **CODE:** `detectCapabilities(hostId, declared)` (`src/host/capabilities.ts:8-14`) falls back to `BASELINE = { embeddedUI: false, subscriptions: false }` when nothing is declared; `describeCapabilities` (`capabilities.ts:16-20`) renders that as `"Browser review (complete fallback experience); polling updates."` — exact match to the claim.
  **VERDICT:** MATCHES
  **FIX:** none

- **CLAIM:** "Distribute V0 as an npm package plus MCP configuration and thin Skill in one installable plugin, published via npm, GitHub, and the MCP Registry." / "A thin optional Skill teaches an agent when the Visual Direction Loop is preferable to prose... The MCP server remains self-describing and useful without the Skill." — `docs/background/product-strategy.md:151,173`
  **CODE:** Matches current shipped state (`package.json:14-16`, `skills/visual-intent/SKILL.md`) but contradicts settled decision #2 (Skill deletion) and is partly still forward-looking correctly (decision #6 confirms the MCP Registry listing is being added, so that half of the sentence is *not* stale).
  **VERDICT:** STALE (Skill clause only; MCP Registry clause is on-target)
  **FIX:** `docs/background/README.md` already frames this document as "kept for provenance" with current behavior documented in root `README.md`/`design.md`/`CONTEXT.md`, so a full supersession note is optional. If one is added, scope it narrowly to the "thin Skill" clause in these two sentences, leaving the MCP Registry and npm/GitHub distribution claims alone since decision #6 confirms them.

- **CLAIM (Outcome section):** "Adopted in the surface-refinement iteration: one full-height rail, and a floating island at the stage's lower edge holding two icon-only tiles — point and box..." — `docs/background/mode-and-toolbar-research.md` (Outcome section, near end of file)
  **CODE:** `src/ui/styles/shell.css` has `.rail` (line 252), `.mode-island` (line 173), `.island-host` (line 165), and an `--island-tile` token (line 187/tokens.css:56 `--rail-width`), consistent with the described rail + floating island layout with no top bar remaining ("The top bar is gone").
  **VERDICT:** MATCHES
  **FIX:** none

## Code with no documentation

- `src/cli-setup.ts:296-473` (`planCodex`, `planClaudeGlobal`, `planOpencode`) — native per-harness MCP registration (Codex TOML table read/repair, `claude mcp add-json --scope user` delegation, opencode JSON/JSONC handling with a comment-detector that refuses to touch commented config). This is load-bearing right now (it's what `visual-intent setup` actually does) but is scheduled for deletion under decision #1. `harness-mcp-setup-research.md` describes only the aspiration (section 10) without ever confirming it landed — worth noting in case the supersession note is written before this code is actually removed, so a reader isn't confused about why "recommendation" language describes existing behavior.
- `src/cli-setup.ts:475-500` / `formatStatus` and the `--status` flag (`src/cli.ts:14,72-75`) — a full detection-and-registration status report ("registrations: … evidence: …") is implemented and is the concrete result of ADR-0014's "content-verified registration" consequence, but neither `harness-mcp-setup-research.md` nor `pi-validation.md` mentions `--status` at all. Not urgent to document here since ADR-0014 covers it and this whole surface is being deleted, but flagged since it's real, currently-shipped, undocumented-in-my-lane behavior.

## Could not verify

- Whether `pi-mcp-adapter` reads `.mcp.json` / `~/.config/mcp/mcp.json` as claimed in `harness-mcp-setup-research.md` §6 and relied on by `pi-validation.md:15`. The research document itself already flags this as unverified (both candidate GitHub URLs 404'd at research time). I have no network access and nothing under `src/**` implements or tests the pi-mcp-adapter side of this, so I cannot settle it from the code either. Settling it would require either live access to a `pi-mcp-adapter` install or its source repository.
- Whether the `mcp.json` file at repo root (`mcp.json:1-8`, still present, still listed in `package.json:20` `files`) and its release-time check are in fact scheduled for the same deletion as decision #3 states — I confirmed the file and its packaging reference exist today, and confirmed `.github/workflows/publish.yml` contains no distinct check keyed to `mcp.json` (only the `npm publish` steps), but I could not find the "release-time check" decision #3 refers to anywhere in `.github/**` or `package.json` scripts to confirm exactly what mechanism is being removed. This is outside my lane (not a background/pi-validation doc claim) so I did not chase it further, but flagging it since a supersession note author for `harness-mcp-setup-research.md` may want the precise mechanism name.