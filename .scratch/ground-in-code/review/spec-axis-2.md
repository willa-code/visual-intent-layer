## Review

**Scope:** SPEC fidelity of tickets 04 (dead weight), 05 (browser opener), 07 (unverified claims), verified against current `src/**`, `package.json`, `README.md`, `design.md`, `CONTEXT.md`.

- **Correct — Ticket 04, dev tooling:** `package.json:30-37` now reads `["dist","!dist/benchmark","!dist/eval","!dist/instrumentation","schema","skills","mcp.json"]`. `src/cli.ts` and `src/mcp/stdio.ts` import nothing from `benchmark/eval/instrumentation` (only `src/instrumentation/measure.ts` imports `benchmark`, and that whole tree is excluded together), so neither bin entry loses anything. I have no shell tool to execute `npm pack --dry-run --json` myself, so the 209→181/28-file arithmetic in the ticket's Comments is arithmetically consistent and structurally supported (dist already contains the three excluded trees) but not independently re-run here — report this as a verification gap, not a defect.
- **Correct — Ticket 04, `PassState`:** `src/annotation/store.ts:32` is now `'in-flight' | 'ready' | 'closed' | 'withdrawn'`; no `'open'`. `src/ui/gallery.ts:245` renders `['in-flight','ready','closed','withdrawn']`, matching the ticket's fix exactly.
- **Correct — Ticket 04, gallery/fixtures decision:** `README.md:305-306` states the gallery is at `/gallery`, "not part of the product's navigation," pinned by `tests/gallery-snapshot.test.ts` — matches the ticket's stated rationale for leaving both as-is.
- **Correct — Ticket 05, browser opener:** `src/service/browser.ts` attaches `once('error'...)`/`once('spawn'...)` before returning, catches a synchronous throw, and resolves a boolean. `src/mcp/service.ts` has exactly four `return` statements in `openArtifact` (lines 196, 217, 243, 264), each setting `browserOpened: await openBrowserIfAsked(...)` — all four return paths covered, matching "the four call sites... use the result." `mcp/server.ts:67-69` and `cli.ts:172-175` both surface the failure sentence; `README.md:93-98` and `.agents/skills/.../open-artifact.md` (`open-no-browser` sub-feature, Gotchas note on no live drive) both document it as claimed.
- **Correct — Ticket 07, claim 1 (envelope versions):** `src/envelope/validate.ts:29-30` — `CURRENT_VERSION='0.4'`, `READABLE_VERSIONS=['0.4','0.3','0.2']`. `README.md:256-262,321` now states 0.4 current, 0.2/0.3 read-for-compatibility, 0.1 refused/historical — matches code and `envelope.test.ts`'s two readability cases (`0.2` at :76, `0.3` at :126).
- **Correct — Ticket 07, claim 4 (design.md §3 tokens):** `design.md`'s type/metrics/radius rows now say `type.rail.title`, "`type.body` at 600... composed, not a token," icon glyph `18`, mode-island tile `36`, and radius `pill (999px)` with no `full`/`type.body.strong`/`type.panel.title` remaining — all match `tokens.css`'s actual variables (`--type-rail-title`, `--icon-size:18px`, `--island-tile:36px`, `--radius-pill:999px`, no `--radius-full`).
- **Correct — Ticket 07, claim 6:** `CONTEXT.md:75-77` Certified Experience now states "No combination is certified yet"; Baseline Compatibility left unchanged, as the ticket's Comments describe.

No requirement from tickets 04/05/07 found missing, no scope creep, no implementation that looks right but is wrong.

Merge verdict: **OK**