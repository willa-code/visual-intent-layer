## Review

**Scope of this pass:** verified the six ticket-claimed doc fixes in `5bf096a` directly against the named source lines (`src/artifact/fidelity.ts`, `src/service/http.ts`, `src/ui/app.ts`, `src/annotation/store.ts`, `src/resolution/model.ts`, `src/ui/components.ts`, `.github/workflows/ci.yml`, `scripts/check-records.js`, `src/mcp/service.ts`, `src/ui/artifact/boundary.ts`).

- **Correct:** `SECURITY.md`'s directive list matches `applicationContentSecurityPolicy` (`src/artifact/fidelity.ts:229-246`) exactly — all 13 directives plus the sandbox token, none missing, none invented. The "two sentences" framing matches `http.ts:931`'s `note` field, which is literally two sentences.
- **Correct:** `design.md:284` (`AnnotationCard`) — "the target's own label, its accessible name, or its semantic role, in that order, falling back to the kind only where the artifact offers none" matches `describeTarget` (`src/ui/app.ts:2102-2107`): `target.label ?? grounding.accessibleName ?? grounding.semanticRole ?? target.kind`.
- **Correct:** `design.md:172,175,287,293` (Pass/Resolution/AnnotationPill vocabulary) matches `PassState` (`store.ts:32`: `open|in-flight|ready|closed|withdrawn`) and `ResolutionLabel` (`model.ts:3`: `matched|recovered|ambiguous|deleted|state-only|blocked`) exactly; `design.md:272` still names `open` and is correctly left as the acknowledged, out-of-scope survivor (findings.md §Comments).
- **Correct:** ADR-0002/0012/0019 headers accurately name the superseding ADRs and the retired/added verbs and states, cross-checked against `service.ts:606-673` (four tools) and `store.ts:32` (`withdrawn`, unwritten `open`).
- **Correct:** `reach-within-the-class.md`'s `reach-refusal-unloaded` quote — "lives inside a frame that has not loaded, so there is nothing inside it to find yet" — is verbatim from `src/ui/app.ts:2157` (the ticket text cites `:2156`, an off-by-one in the ticket itself, not caused by this diff). `annotate-and-send.md`'s `No note` and the Preflight command order in `maintain-visual-intent-layer/SKILL.md:74` match `app.ts:460` and `ci.yml:19-21`.

- **Finding (P2):** `CONTEXT.md:47` — spec.md issue 01 asks to "replace the `supersedes` claim with the real mechanism: a Replacement reaches the agent as `replacedId`/`replacementId` in the `check_in` result." The diff only deletes the false `supersedes` clause; `CONTEXT.md:47-48` now states the Replacement *relationship* but never mentions `replacedId`/`replacementId`. A repo-wide grep for `replacedId|replacementId` across `CONTEXT.md`, `design.md`, and `docs/**` finds only the source at `src/mcp/service.ts:72,469` — no document states it. The ticket's own Comment ("the Replacement mechanism stays documented where it belongs") is unverified by evidence; it isn't documented anywhere. Smallest fix: add one clause to `CONTEXT.md:47` or `design.md` naming `replacedId`/`replacementId` on the `check_in` result.

No scope creep found: `SECURITY.md`'s stale `0.3.0-next.x`/`next`-tag sentence (findings.md row 14, deferred to the distribution spec) is untouched, and dated amendment bodies in `design.md` (e.g. `:702`, still `rejected/not-fixed/...`) were correctly left unrewritten per the historical-record rule.

**Merge verdict:** OK with notes.