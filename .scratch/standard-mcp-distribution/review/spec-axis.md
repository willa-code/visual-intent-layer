## Review

- Correct: `setup` is genuinely unreachable — `src/cli.ts:1-135` dispatches only `mcp`/`serve`/`open` and errors on anything else; `find` for `*setup*`/`*harness*` under `src/` returns nothing, and repo-wide grep for `cli-setup|harness-registry|skills/visual-intent|--no-skill|visual-intent setup` hits only historical docs/ADRs, matching the spec's own claim (spec.md:227-228).
- Correct: `mcpName` (`package.json:5`) exactly equals `server.json`'s `name` (`server.json:3`), and all three version fields (`package.json:2`, `server.json:8`, `server.json:14`) agree at `0.3.0-next.3`, matching the Bump/Registry steps in `.agents/skills/maintain-visual-intent-layer/SKILL.md`.
- Correct: `serverVersion` is read from `package.json` via `src/version.ts`'s `packageVersion()`, wired through `SERVER_VERSION` (`src/mcp/server.ts:11`) and returned unconditionally from every `open_visual_review` call (`src/mcp/service.ts:665`), not just on failure — matches D2. `protocol.test.ts:43-44` asserts it against the live `packageVersion()`.
- Correct: all four Skill triggers are ported verbatim into `open_visual_review`'s description (`src/mcp/service.ts:614`: naming by location, comparing visible things, a prose correction that already missed, wanting to verify visually) — matches D4 and ADR-0031's "four concrete triggers" paragraph.
- Correct: the Skill is gone from the published surface — no `skills/` at repo root, no `pi` key or `pi.skills` in `package.json`, `files` excludes it.
- Correct: the registry flow in `.agents/skills/maintain-visual-intent-layer/SKILL.md` ("Bump"/"Registry listing" sections) matches `mcpName`/`server.json`/`mcp-publisher validate|login|publish` exactly as the doc-sync ticket (06) describes.
- Correct: `.github/workflows/publish.yml:20-25` has a `Refuse a pre-release` step gated on `github.event.release.prerelease` that `exit 1`s, and only publishes to `latest` when the release is not a pre-release — matches ADR-0031's "Releases become official only" and spec.md's Comments claim.
- Correct: `CONTEXT.md` no longer defines **Harness Detection** or **Harness Registration** (grep returns no hits), matching D1; `src/ui/target-label.ts` gives `region`/`text-range` glossary-safe fallback words, matching ticket 08.

- Finding: **P1 — the official MCP Registry listing is not documented as the discovery path in `README.md`**, contradicting an explicit requirement. Location: `README.md:21-59` (the whole Install section). Evidence: `.scratch/ground-in-code/issues/06-sync-documents-for-the-new-install-story.md:11-14` requires the replaced Install section to state "register the MCP server with your own Harness's registrar, the two transport forms to enter by hand, **and the official MCP Registry listing as the discovery path**" — and `docs/adr/0031-...md`'s Solution/user story 4 ("As a Builder-Reviewer, I want to find the server where clients look for MCP servers, so that discovering it does not depend on reading this repository") is the requirement this satisfies. `grep -i "Registry"` against `README.md` returns zero matches anywhere in the file, including the Install section, the Layout list, and "Maintaining this package." The registry-listing mechanics exist correctly elsewhere (`server.json`, `.agents/skills/maintain-visual-intent-layer/SKILL.md`'s "Registry listing" section), but the Builder-Reviewer-facing doc this ticket named never says the listing exists or how to use it to discover the server. Ticket 06's own Comments (lines describing what landed) list README's install-table, transport, CONTEXT.md, SECURITY.md and pi-validation.md changes but never mention adding the Registry-listing sentence — corroborating that this line was dropped rather than merely under-verified. Smallest fix: add one sentence to the Install section naming the `io.github.willa-code/visual-intent-layer` registry listing as where a Registry-aware client can discover the server.

- Finding: P2 — `package.json`'s `keywords` still list `"agent-skills"` and `"pi-package"` (`package.json:15-16`) after the Skill and packaged-manifest deletion. Not a behavior bug and not covered by any D-decision text, but it leaves npm-search metadata claiming a capability ADR-0031 explicitly removed ("the product ships no Skill"). Smallest fix: drop both keywords in the next version bump.

Merge verdict: OK with notes — one P1 documentation gap against an explicit, named requirement (issue 06 / ADR-0031 user story 4), one P2 metadata leftover. No code behavior contradicts the spec's D1–D5 decisions or the Verification Run's proven claims; the `setup` deletion, version wiring, trigger porting, and Publish pre-release refusal are all implemented as claimed.