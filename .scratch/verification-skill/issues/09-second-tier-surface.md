# 09: The second tier — the rest of the user-facing surface

**What to build:** The remaining user-facing behaviours are mapped with recipes, so that the map describes the whole product a Builder-Reviewer can use and not only the loop's spine. These are recorded honestly as not yet driven rather than claimed as covered.

**Blocked by:** 08 (The skill body, executed once in its own order)

**Status:** done

- [x] `attachments` covers adding a reference image by each supported path and the visible refusal of a disallowed or oversized one
- [x] `decision-drawer` covers the attention state, the items needing a decision, and the disclosure of what will leave the machine and which origins the Artifact contacts
- [x] `agent-position` covers each position the surface states, as a sentence, and that acknowledgement never reads as verification
- [x] `session-and-overflow` covers reloading the Artifact, copying the Artifact path, copying evidence, and ending the session
- [x] `running-application-mode` covers reviewing a running application through the same surface, names its loopback-only scope, and covers Source Provenance as a separate axis from Rendered Grounding
- [x] `accessibility-and-keyboard` covers the tool keys, the Escape unwinding order, visible focus distinct from selection, and reduced motion
- [x] Every file has the four required sections, its preconditions, exact commands and observable results
- [x] Every file carries the not-yet-driven marker
- [x] Every command in every recipe is accepted by the Lever, and no recipe invents a command the Lever does not have
- [x] Running-application mode names the refusal of a non-loopback or authenticated production origin as a precondition

## Comments

Six files written with the four sections: `attachments`, `decision-drawer`, `agent-position`, `session-and-overflow`, `running-application-mode`, `accessibility-and-keyboard`. Every file carries the `Not yet driven.` marker and uses only Lever commands (`attach`, `attention`, `overflow`, `press`, `launch --app`).
