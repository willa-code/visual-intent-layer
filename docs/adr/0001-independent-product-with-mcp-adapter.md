# Build an independent visual-intent product with MCP as an adapter

The Visual Intent Layer will provide a consistent standalone product experience and use MCP as its principal agent-integration adapter, including an embedded MCP App where hosts support it. This avoids making product behavior depend on uneven host capabilities while preserving interoperability and allowing the Visual Intent Envelope and capture components to evolve into an embeddable SDK.
