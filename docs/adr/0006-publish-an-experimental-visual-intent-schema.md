# Publish an experimental Visual Intent Envelope schema

The product will publish a versioned, experimental Visual Intent Envelope schema early so independent hosts and adapters can test interoperability. It will be described as a product-owned experimental contract, not a standard, until multiple independent implementations and measured reliability justify a broader claim. Backward compatibility, migration rules, and conformance fixtures will accompany any stable release.
