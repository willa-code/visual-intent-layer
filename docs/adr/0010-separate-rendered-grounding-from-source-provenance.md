# Separate rendered grounding from source provenance

The product will remain useful with zero-configuration rendered grounding while treating editable-source provenance as a stronger, separately labelled evidence level. Application adapters may supply exact source evidence; otherwise the product will expose ambiguity or absence and will not promote inferred locations as fact. Initial deep provenance work will target one framework and build stack rather than claiming arbitrary-application coverage.
