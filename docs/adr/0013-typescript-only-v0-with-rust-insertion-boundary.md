# Ship a TypeScript-only V0 with a preserved Rust insertion boundary

V0 is TypeScript-only for server, UI, adapters, and persistence, distributed via npm with Node required. The local-authority boundary stays narrow so a Rust daemon can replace the Node runtime later only if startup, memory, install, or reliability measurements demand it; this preserves iteration speed and avoids a binary matrix before interaction value is proven on pi.
