{
  "mcpServers": {
    "visual-intent-layer": {
      "command": "npx",
      "args": ["-y", "--package", "visual-intent-layer@0.3.0-next.1", "visual-intent-mcp"],
      "env": {
        "VISUAL_INTENT_DATA_DIR": "${HOME}/.visual-intent-layer/data"
      }
    }
  }
}
